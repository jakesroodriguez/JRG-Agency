/**
 * La landing vive en index.html (script inline) para que funcione
 * al abrir el archivo con doble clic (file://).
 *
 * Edita el bloque <script type="text/babel"> dentro de index.html.
 */

export {};
